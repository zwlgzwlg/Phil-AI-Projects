# Server port (optional — defaults to 3000)
PORT=3000

# ANTHROPIC_API_KEY is no longer needed here.
# Players bring their own key via the in-game API Key modal.
# The key is stored in their browser's localStorage and sent
# directly to api.anthropic.com — it never touches this server.
